<script type="text/javascript">
    {% if product.selected_variant %}
        window.location = "/pages/customizer-1?sku={{ product.selected_variant.sku }}";
    {% else %}
        window.location = "/pages/customizer-1?sku={% for variant in product.variants limit:1%}{{variant.sku}}{% endfor%}";
    {% endif %}
</script>